module Version where

import           Prologue

import qualified Data.Version  as Version

-- import qualified Config as Config

fullVersion :: String
fullVersion = "najlepsza wersja testowa" --"Undo version " ++ Version.showVersion Config.version
