var app              = require('./app'),
    common           = require('./common'),
    config           = require('./config');
    atomCallback = require('./atom-callback')();

var run = function() {

GHCJS_CODE_BE_THERE

  h$errorMsg = require("./BSOD").appCrashed;
};

return { start     : function() { run();}
       , pushEvent : function() { atomCallback.pushEvent(); }
       }
